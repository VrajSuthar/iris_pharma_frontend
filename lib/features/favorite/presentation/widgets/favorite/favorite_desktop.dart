import 'package:flutter/material.dart';

class FavoriteDesktop extends StatelessWidget {
  const FavoriteDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
