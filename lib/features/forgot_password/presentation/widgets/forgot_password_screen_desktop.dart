import 'package:flutter/material.dart';

class ForgotPasswordScreenDesktop extends StatelessWidget {
  const ForgotPasswordScreenDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
