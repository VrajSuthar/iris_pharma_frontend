import 'package:flutter/painting.dart';

/// Brand color palette.
abstract final class AppColors {
  /// Soft blue-gray — secondary UI elements, borders, and subtle backgrounds.
  static const softBlueGray = Color(0xFFDBE2EF);

  /// Medium blue — primary accents, buttons, links, and active states.
  static const primaryBlue = Color(0xFF3F72AF);

  /// Deep navy — headings, strong text, and prominent UI elements.
  static const deepNavy = Color(0xFF112D4E);

  /// Warm off-white — main app background.
  static const background = Color(0xFFF9F7F7);
}
